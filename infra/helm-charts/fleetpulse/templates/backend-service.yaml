apiVersion: v1
kind: Service
metadata:
  name: fleetpulse-backend-service
spec:
  selector:
    app: fleetpulse-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
