apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: fleetpulse-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.frontend.port }}
  type: NodePort
