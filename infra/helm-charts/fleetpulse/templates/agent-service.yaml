apiVersion: v1
kind: Service
metadata:
  name: fleetpulse-agent-service
spec:
  selector:
    app: fleetpulse-agent
  ports:
    - protocol: TCP
      port: {{ .Values.agent.port }}
      targetPort: {{ .Values.agent.port }}
  type: ClusterIP