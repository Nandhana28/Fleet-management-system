# Anomaly detection thresholds
SPEED_THRESHOLD = 80        # km/h — above this is overspeeding
FUEL_DROP_THRESHOLD = 15    # % — sudden drop above this is fuel theft
ROUTE_DEVIATION_KM = 2      # km — deviation above this is route deviation

# Coimbatore GPS bounds
COIMBATORE_BOUNDS = {
    "lat_min": 10.95,
    "lat_max": 11.15,
    "lon_min": 76.85,
    "lon_max": 77.10,
}

# ML model thresholds
ML_ANOMALY_SCORE = -0.1     # Score below this is anomalous
ML_CHECK_EVERY_N = 10       # Run ML check every N readings

# Alert severity levels
SEVERITY = {
    "OVERSPEEDING": "HIGH",
    "FUEL_THEFT": "CRITICAL",
    "ROUTE_DEVIATION": "MEDIUM",
    "ML_ANOMALY": "MEDIUM",
}
